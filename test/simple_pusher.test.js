var assert = require('assert');
var simple_pusher = require('../index.js');

module.exports = {
  'test_something': function() {
    //var config = {};
    //var channel = 'test_channel';
    //var event_name = 'test';
    //var data = 'test';

    //assert.ok(simple_pusher.trigger(config, channel, event_name, data));
  }
};
